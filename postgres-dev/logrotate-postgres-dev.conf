/srv/logs/postgres/postgresql.log {
    daily
    rotate 14
    missingok
    notifempty
    compress
    delaycompress
    create 0666 root root
    sharedscripts
    postrotate
        docker kill --signal=USR1 dev-postgres >/dev/null 2>&1 || true
    endscript
}
